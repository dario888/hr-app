


const initState = {
    employeesList: []
}


export default (state = initState, actions) => {

    switch (actions.type) {
        
        
        default:
            return state;
    }
}